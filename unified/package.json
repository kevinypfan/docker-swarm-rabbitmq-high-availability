{
  "name": "rabbitmq-tester",
  "version": "1.0.0",
  "description": "RabbitMQ Testing Tool - Unified Producer/Consumer for HA testing",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start:consumer": "MODE=consumer node index.js",
    "start:producer": "MODE=producer node index.js",
    "start:both": "MODE=both node index.js",
    "start:stats": "MODE=stats node index.js",
    "start:prod": "NODE_ENV=production node index.js",
    "start:dev": "NODE_ENV=development node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "rabbitmq", 
    "ha", 
    "high-availability",
    "producer", 
    "consumer", 
    "docker", 
    "swarm",
    "microservices",
    "message-queue"
  ],
  "author": "kevinypfan",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.10.8",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
